// app/javascript/packs/application.js
import 'bootstrap';
import 'animate';

console.log('Hello World from Webpacker')

// const card = document.querySelector('.card-category');

// card.classList.add('animated', 'flipInY')

// card.addEventListener('animationend', function() { doSomething() })


// const elm = document.querySelector('.card-category');

// // First: get the current bounds
// const first = elm.getBoundingClientRect();

// // execute the script that causes layout change
// doSomething();
//triggerAnimation(card);

// // Last: get the final bounds
// const last = elm.getBoundingClientRect();

// // Invert: determine the delta between the
// // first and last bounds to invert the element
// const deltaX = first.left - last.left;
// const deltaY = first.top - last.top;
// const deltaW = first.width / last.width;
// const deltaH = first.height / last.height;


